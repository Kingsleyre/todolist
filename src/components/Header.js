import React from 'react';

function Header (props){

  // children is what is inside the <Header> tag
  // extra is a JSON object with {className:}
  return (
    <>
    <h1>Kingsley's  TodoList</h1>
    </>

   
  );
}

export default Header;